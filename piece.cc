#include <string>
#include "piece.h"

using namespace std;
   
Piece(string type, int row, int col){
    this -> type;
    this -> row;
    this -> col;
}
int getRow(){
    return row;
}
int getCol(){
    retrun col;
}